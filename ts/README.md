# planir

Official SDK for [planir.io](https://planir.io) — hosted, hardware-isolated microVM runtimes with a stable public URL per runtime.

This is a placeholder release reserving the package name. The real SDK (generated TypeScript client: `PlanirClient`) ships with the public beta.
