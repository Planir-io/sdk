// This file was auto-generated by Fern from our API Definition.

/**
 * Detached execs only (sync execs are request-scoped, never listed). Records are in-process: they expire past the brief post-exit retention and die with an engine restart — the same ephemerality the poll endpoint documents.
 */
export interface ExecList {
    execs: ExecList.Execs.Item[];
}

export namespace ExecList {
    export type Execs = Execs.Item[];

    export namespace Execs {
        export interface Item {
            /** Poll handle — GET /v1/runtimes/{id}/exec/{execId}. */
            execId: string;
            /** running = still executing; exited = final (briefly retained). */
            status: Item.Status;
            /** The exact argv this exec was started with (echoed). */
            command: string[];
        }

        export namespace Item {
            /** running = still executing; exited = final (briefly retained). */
            export const Status = {
                Running: "running",
                Exited: "exited",
            } as const;
            export type Status = (typeof Status)[keyof typeof Status];
        }
    }
}
