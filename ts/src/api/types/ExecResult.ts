// This file was auto-generated by Fern from our API Definition.

export interface ExecResult {
    /** Process exit code, or null when the outcome is unknown (the command never ran as a process, the compute died under it before reporting, or the capture deadline cut it — `timedOut` says which). A null exitCode is terminal for that invocation — the command is never auto-re-run. */
    exitCode: number | null;
    /** Captured stdout, truncated at 1 MiB (a marker line is appended when truncated). */
    stdout: string;
    /** Captured stderr, truncated at 1 MiB (marker appended when truncated). */
    stderr: string;
    /** True when the capture deadline cut this exec: exitCode is null, the streams hold what was captured up to the cut, and the process MAY STILL BE RUNNING in the runtime (the deadline bounds capture, never the process). False on every other outcome. */
    timedOut: boolean;
}
