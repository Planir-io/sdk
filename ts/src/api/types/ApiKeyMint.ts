// This file was auto-generated by Fern from our API Definition.

import type * as PlanirApi from "../index.js";

export interface ApiKeyMint extends PlanirApi.ApiKey {
    /** The plaintext API key — returned exactly once, at mint, and never retrievable again. Send it as `Authorization: Bearer <secret>`. Store it securely now; the server keeps only a one-way digest. */
    secret: string;
}
