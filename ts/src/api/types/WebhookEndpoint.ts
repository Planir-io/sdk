// This file was auto-generated by Fern from our API Definition.

import type * as PlanirApi from "../index.js";

export interface WebhookEndpoint {
    /** Stable endpoint id — the handle for the endpoint routes. */
    id: string;
    /** The receiver URL. */
    url: string;
    /** The event-type filter; absent = all lifecycle types. */
    eventTypes?: PlanirApi.WebhookEventType[] | undefined;
    /** false = deliveries are stopped (auto-disabled after sustained failure, per the delivery policy). Re-enable explicitly via POST …/{id}/enable; pending deliveries resume. */
    enabled: boolean;
    /** Display prefix of the signing secret: `whsec_` + the first 4 characters — enough to recognise it, never enough to verify with. */
    prefixHint: string;
    /** Set while the endpoint is on the auto-disable clock: the instant its current failure streak began (a delivery exhausted its retry schedule). Null = healthy (any successful delivery clears it). When it grows older than the disable window, the endpoint flips to `enabled: false`. */
    failingSince: string | null;
    createdAt: string;
}
