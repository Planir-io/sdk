// This file was auto-generated by Fern from our API Definition.

export interface ApiKey {
    /** Stable key id — the handle for `DELETE /v1/team/keys/{id}`. */
    id: string;
    /** Display prefix: `plnr_` + the first 4 random characters (e.g. `plnr_ab12`) — enough to recognise a key in a list, never enough to use it. */
    prefixHint: string;
    /** The key’s last 4 characters, to disambiguate keys that share a prefix. */
    last4: string;
    /** The optional display name set at mint; null when unnamed. */
    name: string | null;
    /** When the key was minted (ISO-8601 UTC). */
    createdAt: string;
    /** Approximate time of the key’s most recent authenticated request (best-effort, throttled to ≤ 1 update/minute); null until first used. */
    lastUsedAt: string | null;
    /** When the key expires, if an expiry was set at mint; null = never expires (the machine-credential norm). An expired key fails authentication exactly like a revoked one. */
    expiresAt: string | null;
    /** When the key was revoked; null = active. A revoked key is preserved for audit and fails authentication on its next request. */
    revokedAt: string | null;
}
