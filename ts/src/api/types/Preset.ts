// This file was auto-generated by Fern from our API Definition.

export interface Preset {
    /** Preset id — the shape to request: a runtime whose `resources` exactly match a preset's cpu/memory is billed at that preset's rates. */
    id: string;
    /** Display grouping (`co` = Cost Optimized, shared vCPU; `gp` = General Purpose, dedicated vCPU). Grouping only — availability is not decided by family. */
    family: Preset.Family;
    /** Customer workload container CPU quota in millicores (1000 = 1 vCPU of quota). */
    cpuMillis: number;
    /** Customer workload container hard memory limit in bytes. */
    memoryBytes: number;
    /** Metered compute rate per hour, in microcents (1 USD = 100,000,000). */
    hourlyMicrocents: number;
    /** Per-runtime compute cap per UTC calendar month, in microcents: however long the runtime runs within a month, its compute line never exceeds this. */
    monthlyCeilingMicrocents: number;
    /** Volume storage rate per GiB (2^30 bytes) per month, in microcents. */
    storageMicrocentsPerGibMonth: number;
    /** Free volume allowance per runtime, in bytes — storage bills only the excess. */
    storageFreeBytes: number;
}

export namespace Preset {
    /** Display grouping (`co` = Cost Optimized, shared vCPU; `gp` = General Purpose, dedicated vCPU). Grouping only — availability is not decided by family. */
    export const Family = {
        Co: "co",
        Gp: "gp",
    } as const;
    export type Family = (typeof Family)[keyof typeof Family];
}
