# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.registry_credential import RegistryCredential
from ..types.registry_credentials_list import RegistryCredentialsList
from .raw_client import AsyncRawRegistryCredentialsClient, RawRegistryCredentialsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class RegistryCredentialsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawRegistryCredentialsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawRegistryCredentialsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawRegistryCredentialsClient
        """
        return self._raw_client

    def list_registry_credentials(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> RegistryCredentialsList:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RegistryCredentialsList
            The team's credentials — metadata only, never a password.

        Examples
        --------
        from planir import PlanirClient

        client = PlanirClient(
            token="YOUR_TOKEN",
        )
        client.registry_credentials.list_registry_credentials()
        """
        _response = self._raw_client.list_registry_credentials(request_options=request_options)
        return _response.data

    def create_registry_credential(
        self, *, host: str, username: str, password: str, request_options: typing.Optional[RequestOptions] = None
    ) -> RegistryCredential:
        """
        From now on, EVERY pull from this host on the team authenticates with it — auto-matched by image host, public images included, no anonymous fallback while it exists (delete restores anonymous pulls). Existing runtimes self-heal: a runtime wedged on `ImagePullBackOff` retries onto the new credential within its backoff cycle, no verbs needed. Static basic-auth registries only (Docker Hub, GHCR, GitLab, quay, GAR `_json_key`, ACR service principal); ECR is NOT supported — its 12-hour tokens need node machinery this platform does not run yet.

        Parameters
        ----------
        host : str
            The registry host this credential authenticates, e.g. `ghcr.io`, `docker.io`, `myregistry.example:8443`. Normalized on write (lowercased, scheme stripped, Docker Hub aliases folded to `docker.io`); a path (`ghcr.io/org`) is refused — credentials are host-scoped. At most ONE credential per host: it is used for EVERY pull from that host, public images included (no anonymous fallback while it exists), so a wrong credential blocks that whole host until rotated or deleted.

        username : str
            Registry username — provider-specific (`_json_key` for GAR, `org+robot` for quay).

        password : str
            Registry password / token (write-only — no read ever returns it). Use a scoped, long-lived pull secret: Docker Hub PAT, GHCR PAT (`read:packages`), GitLab deploy token, quay robot token, GAR service-account JSON, ACR service-principal password.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RegistryCredential
            Stored. The password is not echoed — now or ever.

        Examples
        --------
        from planir import PlanirClient

        client = PlanirClient(
            token="YOUR_TOKEN",
        )
        client.registry_credentials.create_registry_credential(
            host="host",
            username="username",
            password="password",
        )
        """
        _response = self._raw_client.create_registry_credential(
            host=host, username=username, password=password, request_options=request_options
        )
        return _response.data

    def get_registry_credential(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> RegistryCredential:
        """
        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RegistryCredential
            The credential — the password is never returned.

        Examples
        --------
        from planir import PlanirClient

        client = PlanirClient(
            token="YOUR_TOKEN",
        )
        client.registry_credentials.get_registry_credential(
            id="id",
        )
        """
        _response = self._raw_client.get_registry_credential(id, request_options=request_options)
        return _response.data

    def rotate_registry_credential(
        self, id: str, *, username: str, password: str, request_options: typing.Optional[RequestOptions] = None
    ) -> RegistryCredential:
        """
        Running runtimes are untouched — pulls read credentials at pull time, so the next pull simply uses the new secret. The host cannot change: it is the credential's identity (one per host) — re-pointing to a different registry is delete + create. A WRONG rotation blocks every pull from this host (public images included) until fixed — visible as the runtime's `observed` waiting reason.

        Parameters
        ----------
        id : str

        username : str
            The replacement username (may be unchanged).

        password : str
            The replacement password / token (write-only). Running runtimes are untouched; the next pull uses it.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RegistryCredential
            Rotated — `updatedAt` reflects it; the next pull uses the new secret.

        Examples
        --------
        from planir import PlanirClient

        client = PlanirClient(
            token="YOUR_TOKEN",
        )
        client.registry_credentials.rotate_registry_credential(
            id="id",
            username="username",
            password="password",
        )
        """
        _response = self._raw_client.rotate_registry_credential(
            id, username=username, password=password, request_options=request_options
        )
        return _response.data

    def delete_registry_credential(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Nothing references a credential (binding is by host match), so delete never 409s. Running runtimes keep running; anonymous pulls from the host resume at the next pull. The latent edge: a stopped runtime on a `:latest`-class private image is re-pulled on its next start, so that start fails visibly — pinned to any other tag, or to a digest, it starts from cached layers.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from planir import PlanirClient

        client = PlanirClient(
            token="YOUR_TOKEN",
        )
        client.registry_credentials.delete_registry_credential(
            id="id",
        )
        """
        _response = self._raw_client.delete_registry_credential(id, request_options=request_options)
        return _response.data


class AsyncRegistryCredentialsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawRegistryCredentialsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawRegistryCredentialsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawRegistryCredentialsClient
        """
        return self._raw_client

    async def list_registry_credentials(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> RegistryCredentialsList:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RegistryCredentialsList
            The team's credentials — metadata only, never a password.

        Examples
        --------
        import asyncio

        from planir import AsyncPlanirClient

        client = AsyncPlanirClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.registry_credentials.list_registry_credentials()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_registry_credentials(request_options=request_options)
        return _response.data

    async def create_registry_credential(
        self, *, host: str, username: str, password: str, request_options: typing.Optional[RequestOptions] = None
    ) -> RegistryCredential:
        """
        From now on, EVERY pull from this host on the team authenticates with it — auto-matched by image host, public images included, no anonymous fallback while it exists (delete restores anonymous pulls). Existing runtimes self-heal: a runtime wedged on `ImagePullBackOff` retries onto the new credential within its backoff cycle, no verbs needed. Static basic-auth registries only (Docker Hub, GHCR, GitLab, quay, GAR `_json_key`, ACR service principal); ECR is NOT supported — its 12-hour tokens need node machinery this platform does not run yet.

        Parameters
        ----------
        host : str
            The registry host this credential authenticates, e.g. `ghcr.io`, `docker.io`, `myregistry.example:8443`. Normalized on write (lowercased, scheme stripped, Docker Hub aliases folded to `docker.io`); a path (`ghcr.io/org`) is refused — credentials are host-scoped. At most ONE credential per host: it is used for EVERY pull from that host, public images included (no anonymous fallback while it exists), so a wrong credential blocks that whole host until rotated or deleted.

        username : str
            Registry username — provider-specific (`_json_key` for GAR, `org+robot` for quay).

        password : str
            Registry password / token (write-only — no read ever returns it). Use a scoped, long-lived pull secret: Docker Hub PAT, GHCR PAT (`read:packages`), GitLab deploy token, quay robot token, GAR service-account JSON, ACR service-principal password.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RegistryCredential
            Stored. The password is not echoed — now or ever.

        Examples
        --------
        import asyncio

        from planir import AsyncPlanirClient

        client = AsyncPlanirClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.registry_credentials.create_registry_credential(
                host="host",
                username="username",
                password="password",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_registry_credential(
            host=host, username=username, password=password, request_options=request_options
        )
        return _response.data

    async def get_registry_credential(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> RegistryCredential:
        """
        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RegistryCredential
            The credential — the password is never returned.

        Examples
        --------
        import asyncio

        from planir import AsyncPlanirClient

        client = AsyncPlanirClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.registry_credentials.get_registry_credential(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_registry_credential(id, request_options=request_options)
        return _response.data

    async def rotate_registry_credential(
        self, id: str, *, username: str, password: str, request_options: typing.Optional[RequestOptions] = None
    ) -> RegistryCredential:
        """
        Running runtimes are untouched — pulls read credentials at pull time, so the next pull simply uses the new secret. The host cannot change: it is the credential's identity (one per host) — re-pointing to a different registry is delete + create. A WRONG rotation blocks every pull from this host (public images included) until fixed — visible as the runtime's `observed` waiting reason.

        Parameters
        ----------
        id : str

        username : str
            The replacement username (may be unchanged).

        password : str
            The replacement password / token (write-only). Running runtimes are untouched; the next pull uses it.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RegistryCredential
            Rotated — `updatedAt` reflects it; the next pull uses the new secret.

        Examples
        --------
        import asyncio

        from planir import AsyncPlanirClient

        client = AsyncPlanirClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.registry_credentials.rotate_registry_credential(
                id="id",
                username="username",
                password="password",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.rotate_registry_credential(
            id, username=username, password=password, request_options=request_options
        )
        return _response.data

    async def delete_registry_credential(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Nothing references a credential (binding is by host match), so delete never 409s. Running runtimes keep running; anonymous pulls from the host resume at the next pull. The latent edge: a stopped runtime on a `:latest`-class private image is re-pulled on its next start, so that start fails visibly — pinned to any other tag, or to a digest, it starts from cached layers.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from planir import AsyncPlanirClient

        client = AsyncPlanirClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.registry_credentials.delete_registry_credential(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_registry_credential(id, request_options=request_options)
        return _response.data
