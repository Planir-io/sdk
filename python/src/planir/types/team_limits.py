# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import UniversalBaseModel
from ..core.serialization import FieldMetadata
from .team_limits_families_value import TeamLimitsFamiliesValue


class TeamLimits(UniversalBaseModel):
    max_concurrent_runtimes: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="maxConcurrentRuntimes"),
        pydantic.Field(
            alias="maxConcurrentRuntimes",
            description="Gate B — the running-runtime count cap (the same value as `package.concurrencyLimit`; breach = 429 CONCURRENCY_LIMIT).",
        ),
    ]
    allowed_preset_ids: typing_extensions.Annotated[
        typing.Optional[typing.List[str]],
        FieldMetadata(alias="allowedPresetIds"),
        pydantic.Field(
            alias="allowedPresetIds",
            default=None,
            description="Gate A — exactly these presets are creatable, regardless of billing mode; null = no explicit set (a metered team must still match SOME public preset; see GET /v1/presets). Breach = 422 INVALID_REQUEST.",
        ),
    ]
    families: typing.Dict[str, TeamLimitsFamiliesValue] = pydantic.Field()
    """
    Gate C — per-family resource ceilings over RUNNING runtimes, region-blind (where each runtime lands stays your choice). A family absent here is uncapped; breach = 429 RESOURCE_QUOTA. A ceiling is an entitlement, never a capacity reservation — a create under quota can still see 503 NO_CAPACITY in a full location.
    """

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
