# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from .webhook_endpoint import WebhookEndpoint


class WebhookMint(WebhookEndpoint):
    secret: str = pydantic.Field()
    """
    The endpoint signing secret (`whsec_` + base64 of 32 random bytes, per the Standard Webhooks spec) — returned exactly once, here, and never retrievable again. Verify deliveries with any Standard Webhooks consumer library: headers `webhook-id`, `webhook-timestamp` (unix seconds; ±5 minutes tolerance recommended), `webhook-signature` (`v1,` + base64 HMAC-SHA256 over `id.timestamp.payload`, keyed with the base64-decoded secret bytes). During a secret rotation the one signature header carries both signatures, space-delimited.
    """

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
