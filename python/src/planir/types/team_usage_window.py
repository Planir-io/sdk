# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import UniversalBaseModel
from ..core.serialization import FieldMetadata
from .team_usage_window_prices import TeamUsageWindowPrices
from .team_usage_window_state import TeamUsageWindowState


class TeamUsageWindow(UniversalBaseModel):
    runtime_id: typing_extensions.Annotated[str, FieldMetadata(alias="runtimeId"), pydantic.Field(alias="runtimeId")]
    period: str = pydantic.Field()
    """
    The UTC calendar month this window covers, `YYYY-MM`.
    """

    metered_ms: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="meteredMs"),
        pydantic.Field(
            alias="meteredMs",
            description="Metered compute milliseconds accrued in the month (event-derived, clipped to the month).",
        ),
    ]
    wall_ms: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="wallMs"),
        pydantic.Field(
            alias="wallMs",
            description="Wall-clock milliseconds the volume existed in the month (create→destroy span, clipped) — accrues while stopped.",
        ),
    ]
    billed_microcents: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="billedMicrocents"),
        pydantic.Field(
            alias="billedMicrocents",
            description="What this window has billed so far, integer microcents. 0 for a flat/root window (quantities accrue, nothing is debited) and for a priceless (non-preset) window.",
        ),
    ]
    prices: typing.Optional[TeamUsageWindowPrices] = pydantic.Field(default=None)
    """
    The window's own carried prices, resolved once when it opened — a reprice or renegotiation never rewrites a month in progress. Null AS A UNIT for a non-preset shape (priceless window): quantities still accrue, billedMicrocents stays 0.
    """

    state: TeamUsageWindowState = pydantic.Field()
    """
    open = still accruing this month; closed = final (destroy or month rollover, past the event-visibility skew bound).
    """

    closed_at: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="closedAt"),
        pydantic.Field(
            alias="closedAt", default=None, description="When the window closed (ISO-8601), or null while open."
        ),
    ]

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
