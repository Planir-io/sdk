# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import UniversalBaseModel
from ..core.serialization import FieldMetadata
from .volume_state import VolumeState


class Volume(UniversalBaseModel):
    id: str
    name: str = pydantic.Field()
    """
    The human handle, unique within the team.
    """

    size_bytes: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="sizeBytes"),
        pydantic.Field(alias="sizeBytes", description="Provisioned size in bytes — the hard device-enforced cap."),
    ]
    delete_with_runtime: typing_extensions.Annotated[
        bool,
        FieldMetadata(alias="deleteWithRuntime"),
        pydantic.Field(
            alias="deleteWithRuntime",
            description="true = a runtime-managed `/data` volume (auto-created at runtime create, deleted with its runtime); false = standalone (runtime destroy detaches it instead — the volume and its data outlive the runtime).",
        ),
    ]
    state: VolumeState = pydantic.Field()
    """
    available = unattached, attachable via runtime create `volumeId`, deletable; attached = held by a runtime for that runtime's whole LIFE (stop does not release it — only destroy does); deleting = a delete in flight.
    """

    runtime_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="runtimeId"),
        pydantic.Field(
            alias="runtimeId",
            default=None,
            description="The holding runtime. Present exactly while `state` is `attached`.",
        ),
    ]
    created_at: typing_extensions.Annotated[
        dt.datetime, FieldMetadata(alias="createdAt"), pydantic.Field(alias="createdAt")
    ]

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
