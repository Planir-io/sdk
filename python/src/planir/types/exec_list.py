# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import UniversalBaseModel
from .exec_list_execs_item import ExecListExecsItem


class ExecList(UniversalBaseModel):
    """
    Detached execs only (sync execs are request-scoped, never listed). Records are in-process: they expire past the brief post-exit retention and die with an engine restart — the same ephemerality the poll endpoint documents.
    """

    execs: typing.List[ExecListExecsItem]

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
