# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import UniversalBaseModel
from ..core.serialization import FieldMetadata
from .preset_family import PresetFamily


class Preset(UniversalBaseModel):
    id: str = pydantic.Field()
    """
    Preset id — the shape to request: a runtime whose `resources` exactly match a preset's cpu/memory is billed at that preset's rates.
    """

    family: PresetFamily = pydantic.Field()
    """
    Display grouping (`co` = Cost Optimized, shared vCPU; `gp` = General Purpose, dedicated vCPU). Grouping only — availability is not decided by family.
    """

    cpu_millis: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="cpuMillis"),
        pydantic.Field(alias="cpuMillis", description="CPU allocation in millicores (1000 = 1 vCPU)."),
    ]
    memory_bytes: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="memoryBytes"),
        pydantic.Field(alias="memoryBytes", description="RAM allocation in bytes."),
    ]
    hourly_microcents: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="hourlyMicrocents"),
        pydantic.Field(
            alias="hourlyMicrocents", description="Metered compute rate per hour, in microcents (1 USD = 100,000,000)."
        ),
    ]
    monthly_ceiling_microcents: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="monthlyCeilingMicrocents"),
        pydantic.Field(
            alias="monthlyCeilingMicrocents",
            description="Per-runtime compute cap per UTC calendar month, in microcents: however long the runtime runs within a month, its compute line never exceeds this.",
        ),
    ]
    storage_microcents_per_gib_month: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="storageMicrocentsPerGibMonth"),
        pydantic.Field(
            alias="storageMicrocentsPerGibMonth",
            description="Volume storage rate per GiB (2^30 bytes) per month, in microcents.",
        ),
    ]
    storage_free_bytes: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="storageFreeBytes"),
        pydantic.Field(
            alias="storageFreeBytes",
            description="Free volume allowance per runtime, in bytes — storage bills only the excess.",
        ),
    ]

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
