# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import UniversalBaseModel
from ..core.serialization import FieldMetadata
from .webhook_delivery_state import WebhookDeliveryState
from .webhook_event_type import WebhookEventType


class WebhookDelivery(UniversalBaseModel):
    id: str = pydantic.Field()
    """
    The delivery row id — the handle for redelivery.
    """

    event_id: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="eventId"),
        pydantic.Field(
            alias="eventId",
            description="The wire event id (`evt_<n>`, monotonic) — equals the delivery's `webhook-id` header and payload `id`, INCLUDING across retries and manual redelivery: deduplicate by this.",
        ),
    ]
    event_type: typing_extensions.Annotated[
        WebhookEventType, FieldMetadata(alias="eventType"), pydantic.Field(alias="eventType")
    ]
    runtime_id: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="runtimeId"),
        pydantic.Field(alias="runtimeId", description="The runtime the event concerns."),
    ]
    occurred_at: typing_extensions.Annotated[
        dt.datetime,
        FieldMetadata(alias="occurredAt"),
        pydantic.Field(alias="occurredAt", description="When the event occurred — the payload `timestamp`."),
    ]
    state: WebhookDeliveryState = pydantic.Field()
    """
    `pending` = scheduled (first attempt or mid-retry); `succeeded` = a 2xx landed; `exhausted` = every scheduled attempt failed (redeliver to retry manually).
    """

    attempts: int = pydantic.Field()
    """
    Attempts made so far.
    """

    next_attempt_at: typing_extensions.Annotated[
        typing.Optional[dt.datetime],
        FieldMetadata(alias="nextAttemptAt"),
        pydantic.Field(
            alias="nextAttemptAt",
            default=None,
            description="The next scheduled attempt while `pending`; null once settled.",
        ),
    ]
    last_status: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="lastStatus"),
        pydantic.Field(
            alias="lastStatus",
            default=None,
            description="HTTP status of the last attempt; null before any attempt or on a non-HTTP failure.",
        ),
    ]
    last_error: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="lastError"),
        pydantic.Field(
            alias="lastError",
            default=None,
            description="Failure class of the last failed attempt (`http_<status>`, `timeout`, `network`, `dns`, `ssrf_denied`); null before any attempt and after a success.",
        ),
    ]
    last_duration_ms: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="lastDurationMs"),
        pydantic.Field(alias="lastDurationMs", default=None, description="Wall-clock of the last attempt."),
    ]
    created_at: typing_extensions.Annotated[
        dt.datetime, FieldMetadata(alias="createdAt"), pydantic.Field(alias="createdAt")
    ]

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
