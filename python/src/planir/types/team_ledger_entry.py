# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import UniversalBaseModel
from ..core.serialization import FieldMetadata
from .team_ledger_entry_kind import TeamLedgerEntryKind


class TeamLedgerEntry(UniversalBaseModel):
    id: str
    kind: TeamLedgerEntryKind = pydantic.Field()
    """
    topup / bonus add to the balance (positive); usage_debit subtracts (negative); adjustment is a signed correction (either way, never zero).
    """

    amount_microcents: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="amountMicrocents"),
        pydantic.Field(
            alias="amountMicrocents",
            description="Signed integer microcents — positive adds to the balance, negative subtracts. Exact for every realistic amount (orders of magnitude below 2^53).",
        ),
    ]
    idempotency_key: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="idempotencyKey"),
        pydantic.Field(
            alias="idempotencyKey",
            description="The row's dedup handle: your own key on a topup/adjustment, or a self-describing `usage:{runtime}:{period}:{total}` on a usage debit — how you correlate a row to the write that produced it.",
        ),
    ]
    stripe_ref: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="stripeRef"),
        pydantic.Field(
            alias="stripeRef",
            default=None,
            description="The Stripe event id on a webhook topup; null on every other row.",
        ),
    ]
    created_at: typing_extensions.Annotated[
        dt.datetime,
        FieldMetadata(alias="createdAt"),
        pydantic.Field(alias="createdAt", description="When the row was appended (ISO-8601 UTC)."),
    ]

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
