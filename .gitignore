node_modules/
dist/
*.egg-info/
__pycache__/
.DS_Store
.fern-out/
fern/.preview/
fern/.definition/
# fern stamps a generation-provenance dir into the emitted package; its originGitCommit
# is inherently stale in this local-generation model (points at gen-time HEAD, always
# behind the commit that would include it). Version provenance lives in fern/generators.yml.
**/.fern/
